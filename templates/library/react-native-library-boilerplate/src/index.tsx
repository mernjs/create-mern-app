import React from 'react';
import { View, Text } from 'react-native';

const FBLoginButton = () => {
	return (
		<View>
			<Text>
				Login with Facebook
			</Text>
		</View>
	)
}

export default FBLoginButton;